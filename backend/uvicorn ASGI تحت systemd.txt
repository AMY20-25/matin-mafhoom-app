uvicorn (ASGI) تحت systemd
نوع اپ (فرانت‌اند موبایل): Flutterبک‌اند: FastAPI + Uvicorn + PostgreSQL (SQLAlchemy/Alembic در مسیر backend). سرویس uvicorn با systemd اجرا می‌شود.فرانت‌اند: اپ موبایل Flutter در مسیر ProjeCt/matin_mafhoom، پکیج‌ها نصب و اپ روی Android اجرا شده.

✅ پس سرویس‌های لازم و فعال برای اپ تو همین‌ها هستن:
uvicorn.service (بک‌اند FastAPI روی پورت 8000)

postgresql.service (دیتابیس)

nginx.service (وب‌سرور و بعداً HTTPS)

cloudflared.service (برای آی‌پی متغیر و دسترسی پایدار)
---
الان سه سرویس اصلیت همه پایدار و اتوماتیک هستن:

cloudflared-api.service → تونل Cloudflare همیشه برقرار می‌شه.

uvicorn.service → بک‌اند FastAPI همیشه روی پورت 8000 ران می‌شه.

nginx.service → سایت اصلی (index.html) همیشه سرو می‌شه.

-------------

📌 یعنی چی؟
سرور FastAPI سالمه.

اتصال به دیتابیس درست کار می‌کنه.

سرویس پیامکی ملی پیامک درست کار می‌کنه.

OTP تولید و ارسال میشه.

تأیید OTP کاربر رو ثبت می‌کنه و JWT صادر میشه.

یعنی همه‌ی سرویس‌ها و سرور الان کاملاً سالم و هماهنگ دارن کار می‌کنن 🎉
